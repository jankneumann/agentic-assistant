"""Integration tests gated on environment variables.

Tests in this directory are NOT collected by default. They are opt-in
via env vars (e.g. ``RUN_GRAPH_TESTS=1``) and require external
credentials. CI does not run them.
"""
