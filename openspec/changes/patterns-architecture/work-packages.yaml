packages:
  - id: wp-main
    name: "patterns-architecture — full scope"
    description: >
      Planning-only proposal establishing the four-layer architectural
      model (framework / harness / patterns / transport), Capability
      declaration system, factory contract, and ADVISE worked example.
      No production code changes.
    tasks:
      - "1.1"
      - "1.2"
      - "1.3"
      - "2.1"
      - "2.2"
      - "2.3"
    priority: 1
    dependencies: []
    scope:
      write_allow:
        - "openspec/changes/patterns-architecture/**"
        - "openspec/roadmap.md"
        - "docs/gotchas.md"
      read_allow:
        - "**"
    verification: tier_b
