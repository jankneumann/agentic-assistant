---
schema_version: 1
item_id: P24
timestamp: '2026-07-16T13:28:27.723307+00:00'
decisions:
- title: Owner review verdicts 1-10 (P24 + P21 jointly)
  outcome: All accepted; verdict 6 pins legacy as_*_tools() shim removal as P17 exit
    criterion; verdict 8 made get_recent_snippets async at protocol level immediately,
    deleting the sync bridge from the hot path (bridge survives only at the host-export
    edge)
- title: Cross-change delta consistency
  outcome: "When two open changes MODIFY the same requirement, make the texts identical\
    \ so archive order cannot fork the spec \u2014 applied to the shared memory-policy\
    \ requirement, verified by diff before archiving P24 then P21"
change_id: capability-protocols-v2
phase: review
recommendations:
- P19 implements ModelProvider against the archived contracts; StaticModelProvider
  default preserves current behavior until then
- "asyncpg pools now live on the harness event loop \u2014 the old fresh-loop caveat\
  \ applies only to the export edge"
---

# Learning: P24

## Decisions
- **Owner review verdicts 1-10 (P24 + P21 jointly)**: All accepted; verdict 6 pins legacy as_*_tools() shim removal as P17 exit criterion; verdict 8 made get_recent_snippets async at protocol level immediately, deleting the sync bridge from the hot path (bridge survives only at the host-export edge)
- **Cross-change delta consistency**: When two open changes MODIFY the same requirement, make the texts identical so archive order cannot fork the spec — applied to the shared memory-policy requirement, verified by diff before archiving P24 then P21

## Recommendations
- P19 implements ModelProvider against the archived contracts; StaticModelProvider default preserves current behavior until then
- asyncpg pools now live on the harness event loop — the old fresh-loop caveat applies only to the export edge
