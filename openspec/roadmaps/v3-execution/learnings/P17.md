---
schema_version: 1
item_id: P17
timestamp: '2026-07-17T14:29:05.947847+00:00'
decisions:
- title: Low-level mcp Server over FastMCP
  outcome: "Same official package, wire-identical; low-level accepts pre-built Tool\
    \ entries so pre-schematized dynamic ToolSpecs render without a translation layer;\
    \ FastMCP reserved for future hand-authored surfaces (resources/prompts/elicitation)\
    \ \u2014 D1 addendum after owner review"
  alternatives_rejected:
  - FastMCP decorators (schema regeneration from synthesized signatures)
  - hand-rolled JSON-RPC
- title: ToolSpec exit criterion met with no shim
  outcome: Legacy as_*_tools removed outright; out-of-tree structural extensions must
    implement tool_specs(); migration also fixed latent MSAF HTTP-tool bug (FunctionTool
    adapter)
change_id: mcp-server-exposure
phase: review
---

# Learning: P17

## Decisions
- **Low-level mcp Server over FastMCP**: Same official package, wire-identical; low-level accepts pre-built Tool entries so pre-schematized dynamic ToolSpecs render without a translation layer; FastMCP reserved for future hand-authored surfaces (resources/prompts/elicitation) — D1 addendum after owner review
  - Rejected: FastMCP decorators (schema regeneration from synthesized signatures)
  - Rejected: hand-rolled JSON-RPC
- **ToolSpec exit criterion met with no shim**: Legacy as_*_tools removed outright; out-of-tree structural extensions must implement tool_specs(); migration also fixed latent MSAF HTTP-tool bug (FunctionTool adapter)
