---
schema_version: 1
item_id: P13
timestamp: '2026-07-17T02:47:13.241312+00:00'
decisions:
- title: Persona .env = scoped in-memory namespace
  outcome: Never os.environ; empty value masks process env; ~30-line parser, no python-dotenv
  alternatives_rejected:
  - os.environ mutation (breaks isolation)
  - python-dotenv dep
- title: BudgetLedger protocol + file persistence; DB ledger deferred
  outcome: 'check_action is sync vs async DB stack; protocol is the PostgresBudgetLedger
    seam; P7 daemon should enable persist: file'
- title: Manifest mismatch blocks without public-module fallback
  outcome: Fail closed on malformed manifest; missing manifest warns; TOCTOU accepted
    (at-rest threat model)
change_id: security-hardening
phase: review
recommendations:
- 'P7 daemon: enable guardrails persist: file so budget ceilings survive restarts'
- 'P25: OpenBao provider slots in via PersonaRegistry(credential_provider_factory=...)'
---

# Learning: P13

## Decisions
- **Persona .env = scoped in-memory namespace**: Never os.environ; empty value masks process env; ~30-line parser, no python-dotenv
  - Rejected: os.environ mutation (breaks isolation)
  - Rejected: python-dotenv dep
- **BudgetLedger protocol + file persistence; DB ledger deferred**: check_action is sync vs async DB stack; protocol is the PostgresBudgetLedger seam; P7 daemon should enable persist: file
- **Manifest mismatch blocks without public-module fallback**: Fail closed on malformed manifest; missing manifest warns; TOCTOU accepted (at-rest threat model)

## Recommendations
- P7 daemon: enable guardrails persist: file so budget ceilings survive restarts
- P25: OpenBao provider slots in via PersonaRegistry(credential_provider_factory=...)
