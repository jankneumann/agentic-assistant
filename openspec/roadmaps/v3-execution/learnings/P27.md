---
schema_version: 1
item_id: P27
timestamp: '2026-07-16T18:31:56.971527+00:00'
decisions:
- title: Simulation = persona config + env vars
  outcome: Per-source FastAPI mock apps expose /openapi.json; existing http_tools
    discovery consumes unchanged; lockstep test binds sim persona to roles preferred_tools
- title: Gate probes gen-eval invocability
  outcome: "Present-but-stub checkout counts as SKIP not failure; real-run path still\
    \ unexercised anywhere \u2014 run once on a machine with the real tools repo"
change_id: eval-simulation-loop
phase: review
recommendations:
- Langfuse-API trace export deferred (DB export is the floor)
- Exported datasets git-ignored by default for privacy
---

# Learning: P27

## Decisions
- **Simulation = persona config + env vars**: Per-source FastAPI mock apps expose /openapi.json; existing http_tools discovery consumes unchanged; lockstep test binds sim persona to roles preferred_tools
- **Gate probes gen-eval invocability**: Present-but-stub checkout counts as SKIP not failure; real-run path still unexercised anywhere — run once on a machine with the real tools repo

## Recommendations
- Langfuse-API trace export deferred (DB export is the floor)
- Exported datasets git-ignored by default for privacy
