---
schema_version: 1
item_id: P19
timestamp: '2026-07-16T16:11:03.483175+00:00'
decisions:
- title: Registry-only model configuration (owner verdict 3)
  outcome: 'Deleted harnesses.<h>.model + StaticModelProvider pre-users (Churn Rule:
    breaking cost at lifetime minimum); models: entries+bindings is the single mechanism;
    personas without models: get a synthesized default registry through the same RegistryModelProvider
    code path'
  alternatives_rejected:
  - keep dual config paths with deprecation window
  - new DefaultModelProvider class (second resolution path)
- title: Consumer bindings generalized early
  outcome: "ModelRequest.consumer is an open string bound via models.bindings \u2014\
    \ harness names today, embeddings/memory consumers (P20/P21) join without protocol\
    \ change"
- title: Implementation exposes contract gaps
  outcome: "model_id wire-identifier field and require_confirmation-denies semantics\
    \ both required spec deltas the contracts phase missed \u2014 implementation review\
    \ remains a spec-quality gate"
change_id: model-provider-routing
phase: review
recommendations:
- 'personas/personal persona.yaml: delete any inert model: key on next submodule touch;
  flat models: shape now fails load with migration pointer'
- 'P20: local GX10 endpoints are registry entries with local-only/private-data-ok
  tags; catalog sync from OpenRouter /models'
- "Runtime mid-conversation failover deferred \u2014 chain honored at construction\
  \ only"
---

# Learning: P19

## Decisions
- **Registry-only model configuration (owner verdict 3)**: Deleted harnesses.<h>.model + StaticModelProvider pre-users (Churn Rule: breaking cost at lifetime minimum); models: entries+bindings is the single mechanism; personas without models: get a synthesized default registry through the same RegistryModelProvider code path
  - Rejected: keep dual config paths with deprecation window
  - Rejected: new DefaultModelProvider class (second resolution path)
- **Consumer bindings generalized early**: ModelRequest.consumer is an open string bound via models.bindings — harness names today, embeddings/memory consumers (P20/P21) join without protocol change
- **Implementation exposes contract gaps**: model_id wire-identifier field and require_confirmation-denies semantics both required spec deltas the contracts phase missed — implementation review remains a spec-quality gate

## Recommendations
- personas/personal persona.yaml: delete any inert model: key on next submodule touch; flat models: shape now fails load with migration pointer
- P20: local GX10 endpoints are registry entries with local-only/private-data-ok tags; catalog sync from OpenRouter /models
- Runtime mid-conversation failover deferred — chain honored at construction only
