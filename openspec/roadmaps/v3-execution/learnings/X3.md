---
schema_version: 1
item_id: X3
timestamp: '2026-07-16T11:32:16.285144+00:00'
decisions:
- title: gen-eval is a consumer, not a dependency
  outcome: Removed from pyproject entirely; external invocation documented in evaluation/README.md
  alternatives_rejected:
  - optional dependency-group (path sources still resolve for all groups)
  - git-source (private-repo auth burden)
  - vendoring (source unavailable in remote env)
- title: agent-framework meta pin unlockable on fresh resolution
  outcome: Moved to agent-framework-core+openai 1.10.x; fixes empty-namespace quirk;
    azure branch degrades via existing ImportError guard
- title: Dependabot triage integration
  outcome: X3 graph surgery structurally removed 15 advisories; 11 targeted bumps
    applied; 7 starlette items pin-blocked, accepted risk on loopback surface
change_id: repo-hygiene
phase: implementation
deviations:
- from_plan: pin agent-framework-core per CLAUDE.md gotcha
  actual: core+openai 1.10.x pins with azure connector absent
  reason: SDK restructured namespace between 1.0 and 1.10; the gotcha predates the
    restructure
recommendations:
- 'P24/P19: MSAF azure_openai support returns only when a connector package ships'
- Relax fastapi<0.116 pin as its own reviewed change to unblock 7 starlette advisories
---

# Learning: X3

## Decisions
- **gen-eval is a consumer, not a dependency**: Removed from pyproject entirely; external invocation documented in evaluation/README.md
  - Rejected: optional dependency-group (path sources still resolve for all groups)
  - Rejected: git-source (private-repo auth burden)
  - Rejected: vendoring (source unavailable in remote env)
- **agent-framework meta pin unlockable on fresh resolution**: Moved to agent-framework-core+openai 1.10.x; fixes empty-namespace quirk; azure branch degrades via existing ImportError guard
- **Dependabot triage integration**: X3 graph surgery structurally removed 15 advisories; 11 targeted bumps applied; 7 starlette items pin-blocked, accepted risk on loopback surface

## Deviations
- Plan: pin agent-framework-core per CLAUDE.md gotcha → Actual: core+openai 1.10.x pins with azure connector absent (reason: SDK restructured namespace between 1.0 and 1.10; the gotcha predates the restructure)

## Recommendations
- P24/P19: MSAF azure_openai support returns only when a connector package ships
- Relax fastapi<0.116 pin as its own reviewed change to unblock 7 starlette advisories
