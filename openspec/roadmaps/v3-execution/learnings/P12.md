---
schema_version: 1
item_id: P12
timestamp: '2026-07-19T11:36:49.625081+00:00'
decisions:
- title: Analytics without tables; chain on identity only
  outcome: Outcomes ride trace_delegation + parent-side interaction record; DelegationContext
    has no chain field (test-asserted); router model denial falls back to deterministic
    scoring, never blocks delegation
change_id: delegation-context
phase: review
---

# Learning: P12

## Decisions
- **Analytics without tables; chain on identity only**: Outcomes ride trace_delegation + parent-side interaction record; DelegationContext has no chain field (test-asserted); router model denial falls back to deterministic scoring, never blocks delegation
